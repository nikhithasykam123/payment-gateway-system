server:
  port: 8083
spring:
  application:
    name: payment-service
  kafka:
    bootstrap-servers: localhost:9092
  redis:
    host: localhost
    port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
